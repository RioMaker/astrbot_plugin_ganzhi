# 本地验证记录

验证日期：2026-09-24。版本：0.1.2。

本次调整干支关系图：普通卡 960×740、个人卡 960×1040、日报 960×1350。天干、地支、全部藏干纵向排布，绿色箭头表示生，红色箭头表示克，虚线双向箭头表示同气；另列干合与支合冲害，干合注明未定化。保留简明宜忌，详细依据由 Agent 数据接口返回。

- `python -m pytest -q`：64 项通过。历法固定例取自 lunar-python 上游公开测试；包括立春交节前后一秒、晚子时两种换日口径、UTC 转换、时辰边界、六十日循环、阳历闰日和非法输入。
- 规则验证：十神固定对照、全部 100 个日干组合、当日环境与时辰分离、五行权重归一、十日主日报、Agent 参考文档、生日不进入模型分析载荷；新增生克箭头正反方向、同气、四柱藏干完整性与本气顺序、横向及纵向关系、干支合冲害提示和重复配对去重。
- 适配与调度验证使用 AstrBot 测试替身：绑定重载/删除、临时输入不覆盖绑定、用户/平台/群隔离、独立会话模式合并为群订阅、群主/群管理员/AstrBot 管理员权限、角色缺失 API 查询、冻结调用前身份、模型故障回退、当日去重、失败重试、生成期间关闭、跨日取消、卸载任务取消。
- 0.1.0 时单独读取并执行官方 `CommandFilter` 的解析实现：36 个根指令/别名/参数组合通过，普通聊天不命中。0.1.1 和 0.1.2 未修改命令路由；此项不代表完成整个 AstrBot 启动加载链。
- `ruff check .` 与 `ruff format --check .`：通过。
- 真实 Pillow PNG：公共四柱、辛巳日柱、20010319 阳历生日、十日主日报。新版个人卡和日报已目视检查，中文显示完整、表格列未重叠；测试校验新版图片尺寸、PNG 解码及固定展示字符的字体覆盖。
- 元数据、配置 Schema 和内置字体检查：通过；部署依赖列于 `requirements.txt`。

预览在 `.dev/previews/`，开发环境在 `.venv/`，都不进入 Git 或安装包。没有启动本地 Web 服务；测试创建的日报任务已关闭。

尚未验证：真实 AstrBot 启动与热重载、OneBot/QQ 实际收图、真实模型工具调用、实际群成员角色事件和 08:00 推送。安装后建议依次执行 `/干支 help`、`/干支`、`/干支 绑定 辛巳`、`/干支`，再由群管理员开启日报并查看状态，最后让 Agent 查询已绑定日主。
