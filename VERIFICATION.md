# 本地验证记录

验证日期：2026-09-24。版本：0.2.2。

本次突出个人日柱及宜忌适用范围，继续使用 ganzhi-rules-v2：普通卡 960×830、个人卡 960×1190、日报 960×1440。个人卡以日柱/日主作大标题，添加“个人日运”标记及深色分析对象条，今日和时辰宜忌均标明个人；公共卡和十日主日报各有类别标记。当前四柱、藏干、图例与计算口径保持一致，详细依据由 Agent 数据接口返回。

- `python -m pytest -q`：93 项通过。历法固定例取自 lunar-python 上游公开测试；包括立春交节前后一秒、晚子时两种换日口径、UTC 转换、时辰边界、六十日循环、阳历闰日和非法输入。
- 规则验证：十神固定对照、全部 100 个日干组合、当日环境与时辰分离、环境与本命资料隔离、十日主日报、Agent 参考文档、生日不进入模型分析载荷；新增生克箭头正反方向、同气、四柱藏干完整性与本气顺序、横向及纵向关系、干支合冲害提示和重复配对去重。
- 适配与调度验证使用 AstrBot 测试替身：绑定重载/删除、临时输入不覆盖绑定、用户/平台/群隔离、独立会话模式合并为群订阅、群主/群管理员/AstrBot 管理员权限、角色缺失 API 查询、冻结调用前身份、模型故障回退、当日去重、失败重试、生成期间关闭、跨日取消、卸载任务取消。
- 0.1.0 时单独读取并执行官方 `CommandFilter` 的解析实现：36 个根指令/别名/参数组合通过，普通聊天不命中。0.1.1 至 0.2.2 未修改命令路由；此项不代表完成整个 AstrBot 启动加载链。
- v2 规则反例：仅同五行不视为同干透出、失令有根与失令无根、四杂气月不一概土旺、根逢冲刑不删除、生成同时记录泄气、中介缺失/仅藏/无根/隔位/受合、制约与通关区分、合化条件齐仍不替换五行、争合保留位置、三合缺支/齐支逢冲、三会与三合区分、自刑需重复支。12 支藏干集合与 lunar-python 表交叉核对；Agent 规则编号与来源目录有效，无旧百分比字段。
- 研读记录：笔记与来源目录已写入，包含章节、访问日期和在线录文修订固定链接。只研读所列章节；自动测试证明实现符合所声明的规则，不证明传统预测的现实效力。
- `ruff check .` 与 `ruff format --check .`：通过。
- 真实 Pillow PNG：公共四柱、辛巳日柱、20010319 阳历生日、甲日主、十日主日报。五种图片已目视检查；个人日柱大标题和宜忌作用范围显眼，生日原日期不进入卡片，仅日干时未补造日支。测试校验新版图片尺寸、PNG 解码、文字回退的适用范围/生日隐私及字体覆盖。
- 元数据、配置 Schema 和内置字体检查：通过；部署依赖列于 `requirements.txt`。

普通查询固定样例为北京时间 2026-09-24 14:00：丙午年、丁酉月、辛丑日、乙未时，示例个人日柱辛巳。图片和 `personal-data.json` 来自同一个报告对象；丑未同气与冲刑同时展示。日报样例使用默认推送时刻 08:00，时柱为壬辰。`.dev/previews/index.html` 汇总五种图片，标明命令、绑定查询等价关系和仅返回文字的入口。

预览在 `.dev/previews/`，开发环境在 `.venv/`，都不进入 Git 或安装包。没有启动本地 Web 服务；测试创建的日报任务已关闭。

尚未验证：真实 AstrBot 启动与热重载、OneBot/QQ 实际收图、真实模型工具调用、实际群成员角色事件和 08:00 推送。安装后建议依次执行 `/干支 help`、`/干支`、`/干支 绑定 辛巳`、`/干支`，再由群管理员开启日报并查看状态，最后让 Agent 查询已绑定日主。
